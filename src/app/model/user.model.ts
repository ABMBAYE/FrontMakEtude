import {Role} from "./role.model";

export class User {
    idUser !: number;
    nomUser !: string;
    prenomUser !: string;
    username !: string;
    password !: string;
    //roles = new Role();
}
